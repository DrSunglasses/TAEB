---

#### Mandatory options ####
# What should be controlling TAEB
ai: 'TAEB::AI::Human'
# How TAEB should communicate with NetHack
interface: Local

#### Interface config #####
# Configure the interface if necessary (telnet and ssh)
#interface_options:
#    Telnet:
#        account: taeb
#        password: pass
#    SSH:
#        account: taeb
#        password: pass

#### Character config #####
# Specify what TAEB should choose when picking a character
#character:
#    role: '*'
#    race: '*'
#    gender: '*'
#    align: '*'

#### Display config #######
# How TAEB should look when you're watching him
# color_method controls how tiles are colored, and glyph_method controls which
# glyphs to display. They can both be changed at runtime as well, see
# doc/debug-commands.txt.
#display:
#    color_method: normal # debug, engraving, pathfind, stepped, time, lit
#    glyph_method: normal # floor

#### Debugging config #####
# Configure various debugging plugins for TAEB here
#debug:
#    console:
#        readline: Gnu
#    irc:
#        server: irc.freenode.net
#        port:   6667
#        channel: "#interhack"
#        name: taeb # defaults to the name of the bot

#### Logging config #######
#logger:
#    twitter:
#        deaths:
#            username: taeb_deaths
#            password: pass
#        errors:
#            username: taeb_errors
#            password: pass

#### Misc config ##########
# Set this to 1 if you want to run a buggy TAEB overnight; it causes TAEB
# to quit instead of saving on errors; if your TAEB is not particularly
# buggy, you might want to leave this this at 0 so the full state is kept.
#unattended: 0

#### External config ######
# Specify other config files to load here - for example, config files
# containing passwords, or config files specific to a certain ai
#other_config:
#    - etc/site_config.yml
